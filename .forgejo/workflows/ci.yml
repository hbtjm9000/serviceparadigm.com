# DEPRECATED — replaced by delivery.yml
# This file kept for reference only. All pipeline logic now lives in delivery.yml.
# Delete this file once the team has confirmed delivery.yml is stable.
on:
  push:
    branches: [ignore-deprecated]
  pull_request:
    branches: [ignore-deprecated]
jobs:
  noop:
    runs-on: docker
    container: crumbs-ci:latest
    steps:
      - run: echo "ci.yml is deprecated — use delivery.yml"
