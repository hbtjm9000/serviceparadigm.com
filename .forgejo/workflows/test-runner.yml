name: Test Runner
on:
  push:
    branches: [main]
  pull_request: []

jobs:
  test-runner:
    runs-on: docker
    steps:
      - name: Check runner works
        run: echo "Runner working" && node --version && uname -a
